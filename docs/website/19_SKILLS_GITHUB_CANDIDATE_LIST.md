# Skills/Tooling Candidate List — GitHub Repos Supplied

Classification of the specific repo list supplied (deduplicated — several URLs were repeated), following the same A–G scheme as [12_SKILL_GAP_ANALYSIS.md](12_SKILL_GAP_ANALYSIS.md). Where I'm not confident what a repo actually does, that's stated plainly rather than guessed at — several of these are unfamiliar enough that a confident-sounding wrong answer would be worse than "I don't know."

None of these were installed, cloned, or added as dependencies while producing this list — classification only, per the brief's own "do not blindly install everything" instruction.

| Repo | Class | Reason |
|---|---|---|
| `greensock/GSAP` | **G** | Unchanged from `12_SKILL_GAP_ANALYSIS.md`: no scroll-scrubbing/complex-timeline need exists on this site, and Framer Motion (already installed, now actually wired into the homepage) covers everything the current creative direction calls for. |
| `bruzethegreat/gsap-master-mcp-server` | **G** | An MCP wrapper around GSAP — inherits the same "not needed" conclusion as GSAP itself, plus it's a small, unofficial community server (unverified maintenance track record) if it were ever needed. |
| `nexu-io/open-design` | **F** | This is the `open-design` MCP server already configured in this environment — and already failing to connect (`ENOENT`, a missing local app bundle path), per `12_SKILL_GAP_ANALYSIS.md`. Not usable as configured; flag for you to fix or remove rather than something to route around. |
| `garrytan/gstack` | **D** | I don't have confident, verified knowledge of what this specific repo provides — noting that honestly rather than guessing. Document as a candidate to look at directly if there's a specific reason it was suggested; not installing on an unconfirmed assumption. |
| `obra/superpowers` | **A** | This is the upstream source of the `superpowers:*` skill family (brainstorming, systematic-debugging, writing-plans, etc.) already active and used in this session. Already installed, already required — no action needed. |
| `motiondivision/motion-vue` | **G** | Vue bindings for the Motion library. This project is React, not Vue — not applicable regardless of the library's quality. |
| `motiondivision/motion` | **A**, with a note | This is the modern, framework-agnostic successor to `framer-motion` (same team, package renamed to `motion`). The site's `package.json` currently depends on `framer-motion` specifically (catalog-pinned), which is what got wired in this session (`components/site/reveal.tsx`) and works correctly. Worth knowing that `motion` is the actively-developed name going forward — a future dependency bump/rename, not an urgent action. |
| `JuliusBrussee/caveman` | **A** | Upstream of the `caveman`/`caveman-*` skill family already available in this session. Already installed. |
| `sickn33/antigravity-awesome-skills` | **G** | A skills collection for the Antigravity coding-agent platform. This session runs in Claude Code, not Antigravity — not applicable to this harness. |
| `affaan-m/ECC` | **D** | Not confident what this repo provides from the name alone — flagged for you to clarify rather than guessed at. |
| `pheralb/svgl` | **G** for now | A real, well-known curated library of *other companies'* brand/tech logos (svgl.app). QualiTracker's site currently shows zero third-party logos by design (no client logos, no "trusted by," per `08_PIVOT_ADDENDUM.md`) and the brand kit defines no icon library beyond the QT symbol. Reconsider only if a future "integrations" or "built with" section is added — not applicable to anything currently planned. |
| `VoltAgent/awesome-design-md` | **D** | A curated reference list (an "awesome list"), not a tool to install — useful to skim for ideas, not something with an install decision attached. |
| `ruvnet/ruflo` | **D** | Not confident what this repo provides — flagged, not guessed at. |
| `Anil-matcha/Open-Generative-AI` | **G** | A generative-AI content/tooling collection. The real brand kit explicitly has no photography or illustration in its visual language (flat colour, type, and data graphics only, per `02_BRAND_SOURCE_OF_TRUTH.md`) — generating imagery would contradict the locked brand system rather than extend it. |
| `rtk-ai/rtk` | **A**, out of this scope | This is "RTK" (Rust Token Killer), already installed and active via your global Claude Code hooks (per your own `~/.claude/RTK.md`). It's a general dev-productivity/token-cost tool, unrelated to website design or frontend work specifically — already in use, nothing to do here. |
| `mattpocock/skills` | **D** | A TypeScript-focused skills/reference collection from a well-known TS educator. Potentially useful general reference; not specific to this website's remaining work. |
| `nextlevelbuilder/ui-ux-pro-max-skill` | **A** | This matches `anthropic-skills:ui-ux-pro-max`, already available as a skill in this session. Already installed. |
| `21st-dev/magic-mcp` | **D** | Unchanged from `12_SKILL_GAP_ANALYSIS.md`: available as skills (`21st-ui-build`/`explore`/`review`) in this session, genuinely useful for open-ended component exploration, not needed here since the real, extracted brand system is already the source of truth. |
| `juliangarnier/anime.js` | **G** | Unchanged: a second animation engine alongside an installed, sufficient one (Framer Motion) is redundancy, not new capability. |
| `higgsfield-ai/skills` (`npx skills add ...`) | **G** | Unchanged: image/video generation tooling, contradicted by the brand's no-photography/no-illustration rule. |
| `fusionjs/fusionjs` | **E** | Uber's FusionJS is a full application framework (a Next.js-class alternative), not a component or utility. Adopting it would mean replacing Vite and the entire build/routing setup — incompatible with "don't unnecessarily replace an existing, working stack" (the brief's own §16 rule), for zero demonstrated benefit. |
| `VoltAgent/awesome-claude-code-subagents` | **D** | A curated subagent-definition reference list. Useful to browse for future engineering-subagent work; not specific to the website. |
| `getagentseal/codeburn` | **D** | Not confident what this repo provides — flagged, not guessed at. |
| `GLips/Figma-Context-MCP` | **G** for this project | A legitimate, well-regarded Figma MCP server in general. Not needed here: the QualiTracker brand's actual Figma file was already faithfully extracted into the `qualitracker-io` Claude Design project (read via `DesignSync` this session), which is the real source of truth already used to build v1 — re-opening the raw Figma file would be re-deriving what's already been extracted correctly. |
| `ComposioHQ/awesome-claude-skills` | **D** | Another curated skills reference list — same treatment as the other "awesome-*" entries. |
| `yanqinJiang/Animate3D` | **G** | An academic research repo for AI-generated 3D character animation. No 3D/WebGL need exists anywhere in the approved creative direction — completely out of scope for a QMS marketing site. |
| `shadcn-ui/ui` | **A/B** | The source of the `components/ui/*` primitives already in the project. Already installed; per `12_SKILL_GAP_ANALYSIS.md`, now largely redundant since the actual pages use hand-rolled components built directly against the real brand tokens instead. |
| `RevenueCat/cat-paywall-swiftui` | **G** | A SwiftUI (iOS native) paywall component library. Different platform entirely — not applicable to a React web project. |
| `i18next/react-i18next` | **C** | The one genuinely new, high-value candidate in this list. The brand kit names Swahili support as a competitive advantage (feature status unverified — see `01_EXISTING_BRAND_AUDIT.md`'s checklist), and the target market spans multiple East African countries. **Not installed now** — there's no confirmed plan yet for a multi-language marketing site — but it's the standard, well-maintained, correct choice for a Vite+React app the moment that work is scoped. Worth remembering specifically for that reason. |

## Net recommendation, unchanged from `12_SKILL_GAP_ANALYSIS.md`

Nothing on this list changes the conclusion: no new animation engine, no 3D/WebGL, no framework replacement, no generative-imagery tooling. The one real addition to keep in mind for later is `react-i18next`, if and when Swahili (or other) localization becomes a confirmed, scoped piece of work — not something to add speculatively today.
